names = ["Maya", "JP", "Erica", "Vincent"]
grades = [100, 99, 98, 80]

#print(names[2], names[3])
#print(f"{names[0]}'s grade is {grades[0]}")

for i in range(len(names)):
    print(f"{names[i]} has a {grades[i]}")

#accessing names using a for loop in a list
#student_names is a variable only used in a list
#for student_names in names:
    #print(student_names) 
